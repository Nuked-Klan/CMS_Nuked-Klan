<div id="RL_global">
    @if({{blockUnikFull}} === true)
        @include(blockUnikCenter)
    @else
    <div id="RL_separatorUnikTop"><!-- No Content --></div>
    @endif
    <div id="RL_content{{bigClass}}">
