<section id="RL_blockUnikTop">
    <!--{{classSliderFull}}-->
        @include(slider, classSliderFull)
        @if({{activeTopMatch}} == 1)
            @include(blockTopMatch)
        @endif
</section>
